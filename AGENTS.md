# AGENTS.md

这个仓库的主要用途是记录和维护个人博客文章，不是一个需要做通用前端、Node.js 或 npm 开发的项目。

在这个仓库里工作的 agent，默认应当优先处理文档编辑、文章结构整理和配图管理。不要因为仓库里存在 Jekyll 主题文件、`package.json` 或模板脚手架，就主动去做 JS、npm、主题模板相关修改，除非用户明确提出这类需求。

## 仓库定位

- 主要内容位于 `_posts/`。
- 大多数用户请求都应优先理解为：修改 Markdown 文章及其引用图片。
- `_layouts`、`_includes`、`_sass`、`assets`、`docs`、`test` 等目录属于主题或演示环境，通常不要主动修改。

## 文章规范

- 文章存放路径采用 `_posts/YYYY/MM/`。
- 文件名格式为 `YYYY-MM-DD-slug.md`。
- 每篇文章开头使用简洁的 YAML front matter，形式如下：

```yaml
---
layout: article
title: 文章标题
tags: 标签
---
```

- 除非用户明确要求新增字段，否则保持现有 front matter 风格。
- 普通文章默认使用 `layout: article`。
- 中文文章标题直接使用中文，不要无故改成英文。
- `tags` 可以是单个标签，也可以是与现有文章一致的空格分隔标签列表。

## 图片规范

- 文章配图优先放在 `img/YYYY/MM/DD/` 下，并与文章日期对应。
- Markdown 图片引用使用站点根路径，例如：

```md
![说明](/img/2025/12/30/example.png)
```

- 为某篇文章新增图片时，优先复用该文章对应日期目录。
- 不要主动重构旧图片目录，除非用户明确要求整理。

## 编辑原则

- 优先做小范围、局部性的文章修改。
- 在没有明确要求重写的情况下，尽量保持作者现有的结构、术语和表达风格。
- 如果要优化文章，优先关注这些方面：
  - 错别字和明显病句
  - 表达是否更清楚
  - 小节结构是否更顺
  - 公式和配图说明是否完整
  - 文中图片引用是否一致

- 不要引入与当前文章无关的重构。
- 不要主动修改站点主题、npm 工具链或 Jekyll 配置，除非用户明确要求。

## 默认行为

- 在需求不明确时，默认把任务理解为“文档相关任务”。
- 优先修改 Markdown 文章和补充图片引用，而不是改模板或构建链路。
- 如果一个请求存在歧义，在动 `_posts` 以外的渲染或构建文件前，先确认是否真的需要修改那些内容。

## 中文技术博客的写作偏好

用户在 2026-09-19 选定 Zartbot、苏剑林、BBuf 作为写作参考。允许借鉴三者的通用叙述方法，保持自己的表达，不复刻标志性措辞或冒充作者。

参考目录：`/Users/yxc/code/own_log/2026/09/19-ai-infra-writing-references/README.md`。目录内有三位作者的阅读摘记、原文链接、短摘录，以及 SM120 的三份评审样稿。先读与当前题目相关的一份，无需每次全部读取。

- **Zartbot**：适合 GPU、网络、系统架构与产品取舍。以工作负载或反常现象引出观点，再解释设计和代价。
- **苏剑林**：适合优化器、Attention、算法细节与公式。围绕一个具体疑问逐步推导，符号和公式随问题引入，用算例落地。
- **BBuf**：适合 CUDA、Triton、框架与源码笔记。从代码现象或真实 trace 入手，沿关键调用关系解释原因，避免无关代码堆积。

默认组合：用 BBuf 式源码推进组织工程文章，用问题与推导连接关键数字，架构延伸采用工作负载视角。用户指定某种侧重时优先遵循；不要每篇都硬套三种方法。

具体写法：

- 从代码、实际问题或疑问开篇，直接进入主题。允许自然口语和有依据的个人判断。
- 用连贯段落把前因后果讲出来。小标题只服务于明显转折，避免每两三句话一节、层层 A/B/C 编号、不断列表总结。
- 已经解释过的结论不再换成表格、流程图、加粗句反复讲；表格只留给真正需要并排比较的数据。
- 核实与阅读可以严格，成文不必像评审报告。重要前提在相关处交代一次，减少“不能据此断言”“不等价于”“最终仍需”等反复打断阅读的措辞。
- 源码、公式和性能结论仍需核实并保留必要来源；“不那么严谨”指行文自然，不是允许技术错误。
- 区分实测、手算、资料结果与推断，但不要每段都附免责声明。不得虚构作者的实验、工作经历、团队内部资料或厂商设计动机。
- 新的风格试写先保留原稿，候选版本放在 own_log 的 review 目录。除非用户要求，不把多个候选都放入 `_posts/`，不自动提交或发布。
